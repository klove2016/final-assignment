<% if notice %>
  <div id="notice"><%= notice %></div>
<% end %>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <h1 class="navbar-brand"> <%="Editing #{@user.name}'s Info" %> </h1>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
              <%= link_to 'Home', users_path, class:"nav-link "  %>
          </li>
          <li class="nav-item">
              <li><%= link_to 'View Availabilities', user_availabilities_path(@user), class:"nav-link " %></li>
          </li>
          <li><%= link_to 'Add Availability', new_user_availability_path(@user), class:"nav-link " %></li>
          <li class="nav-item">
            <%= link_to 'Compare Availablities', availabilities_compare_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to 'New User', new_user_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Exisiting Users
            </a>
            <ul class="dropdown-menu">
              <% @users.each do |user| %>
                <li><%= link_to user.name, user, class: "dropdown-item"%></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </div>
    </div>
</nav>

<%= render 'form', user: @user %>
